package com.emoque.repository;

import com.emoque.model.IntroTask;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IntroTaskRepository extends JpaRepository<IntroTask, String> {
}
